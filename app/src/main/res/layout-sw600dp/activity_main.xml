<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.sunshine.app.MainActivity">

    <LinearLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:orientation="vertical"
        android:background="?attr/colorPrimary"
        android:elevation="@dimen/appbar_elevation"
        app:elevation="@dimen/appbar_elevation"
        >

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />

        <!-- These FrameLayouts are to line the image up correctly with the keyline,
             since we cannot do addition of dimensions/attributes otherwise. -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/abc_list_item_padding_horizontal"
            android:layout_marginLeft="@dimen/abc_list_item_padding_horizontal">

            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/list_icon"
                android:layout_marginLeft="@dimen/list_icon"
                android:paddingStart="@dimen/abc_list_item_padding_horizontal"
                android:paddingLeft="@dimen/abc_list_item_padding_horizontal">

                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="?attr/listPreferredItemHeight"
                    android:src="@drawable/ic_logo"
                    android:layout_gravity="center_vertical"
                    android:contentDescription="@string/app_name" />

            </FrameLayout>

        </FrameLayout>

    </LinearLayout>

    <!--
    This is a two-pane layout for the items master/detail flow.
    -->

    <!-- This is used as a strut to create two columns in our RelativeLayout -->
    <android.support.v4.widget.Space
        android:id="@+id/layout_center"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_centerInParent="true" />

    <fragment
        android:id="@+id/fragment_forecast"
        android:name="com.example.android.sunshine.app.ForecastFragment"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_alignEnd="@id/layout_center"
        android:layout_alignRight="@id/layout_center"
        android:layout_below="@id/appbar"
        tools:layout="@android:layout/list_content"
        />

    <FrameLayout
        android:id="@+id/weather_detail_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignStart="@id/layout_center"
        android:layout_alignLeft="@id/layout_center"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="?attr/actionBarSize"
        android:elevation="@dimen/appbar_elevation"
        app:elevation="@dimen/appbar_elevation"
        android:paddingRight="@dimen/abc_list_item_padding_horizontal"
        android:paddingEnd="@dimen/abc_list_item_padding_horizontal"
        android:paddingBottom="@dimen/abc_list_item_padding_vertical"
        />

</RelativeLayout>
